export function createCountString(villageCount, castleCount, waterCount) {
    return `You have changed the village ${villageCount} times. You have changed the castle ${castleCount} times. You have changed the water feature ${waterCount} times.`;
}